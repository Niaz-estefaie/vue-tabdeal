findLongWords = (text) => {
  const arrayedWords = text.toLowerCase().split(" ");
  const trimmedWords = arrayedWords.map((word) => ());
  console.log(trimmedWords);

  // return result.slice(0, -2);
};
